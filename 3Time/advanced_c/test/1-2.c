#include <stdio.h>
#include <string.h>
typedef struct {
    char name[10];
    int kor;
    int eng;
    int math;
}Data;

int main(void)
{
    Data students[4];
    scanf("%s%d%d%d",students[i].name,
    &(students[i].kor), &(students[i].math),&(students[i].eng));
}