/*
    function(int a, int b) -> a,b 를 parameter라고 부른다.
    parameter로 두 수를 받아서 두 수의 합을 return하는 함수를 만들고
    4개의 수에 대해 sum을 두번 실행하고 각 실행 결과가 둘다 3이면 both three
    하나만 3이면 only one three
    아니면 no three를 출력하시오
    sum(a,b)
    sum(c,d)
*/