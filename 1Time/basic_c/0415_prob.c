#include <stdio.h>
#include <time.h>
#include <stdlib.h>

int createRandomNumber()
{

}

int getWinner(int a, int b) // if a wins, return -1 \ if b wins, return 1
{

}

int main(void) 
{
    srand(time(NULL));
    /* 삼판 이선승제로 컴퓨터와 대결을 한다. 한명이 (컴퓨터는 유저든) 
    2번 연속으로 이기면 바로 종료 (3번째 판 안함)
    1,2라운드에서 한번 이기고 한번 지면 3번째 판까지.
    최종 우승자와, 전적을 출력한다.
    */   
}